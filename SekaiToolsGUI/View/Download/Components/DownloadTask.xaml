<UserControl x:Class="SekaiToolsGUI.View.Download.Components.DownloadTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SekaiToolsGUI.View.Download.Components"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:DownloadTask}"
             HorizontalAlignment="Stretch">
    <ui:Card Name="Control" HorizontalAlignment="Stretch">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Name="TaskNameTextBlock"  TextWrapping="Wrap" />
            <Grid Grid.Row="1" Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:SymbolIcon Grid.Column="0" Name="StatusIcon" Symbol="ArrowDownload24"
                               Margin="0,0,5,0" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Name="StatusText" Text="等待下载"
                           VerticalAlignment="Center" />
                <Button Grid.Column="2" Name="RemoveButton" Content="移除"
                        Padding="8,3" Click="RemoveButton_OnClick"
                        AutomationProperties.Name="从下载列表移除" />
            </Grid>
        </Grid>
    </ui:Card>
</UserControl>