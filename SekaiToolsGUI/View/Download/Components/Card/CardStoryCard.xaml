<UserControl x:Class="SekaiToolsGUI.View.Download.Components.Card.CardStoryCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" d:DesignWidth="200">
    <ui:Card VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Padding="10,5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Image Width="24" Height="24" Name="IconImage" Margin="0,0,10,0" />

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Name="TextBlockTitle"
                               Text="# 1" />
                    <TextBlock Name="TextBlockName" FontWeight="DemiBold"
                               Text="★2 XXX" />
                </StackPanel>

            </StackPanel>

            <Grid Grid.Column="2" Name="PanelItems" HorizontalAlignment="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
            </Grid>
        </Grid>
    </ui:Card>
</UserControl>