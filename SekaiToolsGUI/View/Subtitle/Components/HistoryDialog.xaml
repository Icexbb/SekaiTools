<ui:ContentDialog x:Class="SekaiToolsGUI.View.Subtitle.Components.HistoryDialog"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:local="clr-namespace:SekaiToolsGUI.View.Subtitle.Components"
                  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                  mc:Ignorable="d"
                  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                  IsPrimaryButtonEnabled="False" DefaultButton="Primary"
                  Title="历史记录">
    <ui:ContentDialog.Resources>
        <!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}"
               TargetType="{x:Type local:HistoryDialog}" />
    </ui:ContentDialog.Resources>
    <ui:ContentDialog.Content>
        <StackPanel Name="StackPanel"></StackPanel>


    </ui:ContentDialog.Content>
</ui:ContentDialog>