<ListBox x:Class="SekaiToolsGUI.Control.SelectorBar"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         xmlns:local="clr-namespace:SekaiToolsGUI.Control"
         mc:Ignorable="d"
         d:DesignHeight="48" d:DesignWidth="420"
         MinHeight="40"
         Padding="0"
         BorderThickness="0"
         Background="Transparent"
         Foreground="{DynamicResource TextFillColorPrimaryBrush}"
         HorizontalContentAlignment="Stretch"
         VerticalContentAlignment="Stretch"
         SelectionMode="Single">
    <ListBox.Resources>
        <ItemsPanelTemplate x:Key="HorizontalSelectorBarItemsPanel">
            <UniformGrid Rows="1" />
        </ItemsPanelTemplate>
        <ItemsPanelTemplate x:Key="VerticalSelectorBarItemsPanel">
            <UniformGrid Columns="1" />
        </ItemsPanelTemplate>

        <Style x:Key="SelectorBarItemStyle" TargetType="ListBoxItem">
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Padding" Value="12,7,12,9" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Background" Value="{DynamicResource SubtleFillColorTransparentBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Grid>
                            <Border x:Name="ItemBackground"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4" />
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentStringFormat="{TemplateBinding ContentStringFormat}" />
                            <Border x:Name="SelectionIndicator"
                                    Height="3" MinWidth="18" Margin="8,0,8,2"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Bottom"
                                    Background="{DynamicResource AccentFillColorDefaultBrush}"
                                    CornerRadius="2"
                                    Opacity="0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform x:Name="SelectionIndicatorScale"
                                                    ScaleX="0.55" ScaleY="0.55" />
                                </Border.RenderTransform>
                            </Border>
                            <Border x:Name="FocusBorder"
                                    Margin="1"
                                    BorderBrush="{DynamicResource FocusStrokeColorOuterBrush}"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Opacity="0" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBackground" Property="Background"
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicator"
                                                             Storyboard.TargetProperty="Opacity"
                                                             From="0" To="1" Duration="0:0:0.12" />
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicatorScale"
                                                             Storyboard.TargetProperty="ScaleX"
                                                             From="0.55" To="1" Duration="0:0:0.18">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicatorScale"
                                                             Storyboard.TargetProperty="ScaleY"
                                                             From="0.55" To="1" Duration="0:0:0.18">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut" />
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicator"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.08" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type local:SelectorBar}}}"
                                         Value="Vertical">
                                <Setter TargetName="SelectionIndicator" Property="Width" Value="3" />
                                <Setter TargetName="SelectionIndicator" Property="Height" Value="Auto" />
                                <Setter TargetName="SelectionIndicator" Property="MinWidth" Value="0" />
                                <Setter TargetName="SelectionIndicator" Property="Margin" Value="2,8,0,8" />
                                <Setter TargetName="SelectionIndicator" Property="HorizontalAlignment" Value="Left" />
                                <Setter TargetName="SelectionIndicator" Property="VerticalAlignment" Value="Stretch" />
                            </DataTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsKeyboardFocusWithin" Value="True" />
                                    <Condition Property="IsSelected" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="FocusBorder" Property="Opacity" Value="1" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground"
                                        Value="{DynamicResource TextFillColorDisabledBrush}" />
                                <Setter TargetName="SelectionIndicator" Property="Background"
                                        Value="{DynamicResource AccentFillColorDisabledBrush}" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Orientation, RelativeSource={RelativeSource AncestorType={x:Type local:SelectorBar}}}"
                             Value="Vertical">
                    <Setter Property="Margin" Value="0,0,0,4" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </ListBox.Resources>

    <ListBox.Style>
        <Style TargetType="{x:Type local:SelectorBar}">
            <Setter Property="ItemsPanel" Value="{StaticResource HorizontalSelectorBarItemsPanel}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="ItemsPanel" Value="{StaticResource VerticalSelectorBarItemsPanel}" />
                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </ListBox.Style>
    <ListBox.ItemContainerStyle>
        <StaticResource ResourceKey="SelectorBarItemStyle" />
    </ListBox.ItemContainerStyle>
    <ListBox.Template>
        <ControlTemplate TargetType="ListBox">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    ClipToBounds="True">
                <ScrollViewer Padding="{TemplateBinding Padding}"
                              HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                              VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                              CanContentScroll="True"
                              PanningMode="Both"
                              Focusable="False">
                    <ItemsPresenter HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch" />
                </ScrollViewer>
            </Border>
        </ControlTemplate>
    </ListBox.Template>
</ListBox>
