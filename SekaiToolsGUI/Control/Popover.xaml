<ContentControl x:Class="SekaiToolsGUI.Control.Popover"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:local="clr-namespace:SekaiToolsGUI.Control"
                Focusable="False"
                Padding="16"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
    <ContentControl.Template>
        <ControlTemplate TargetType="{x:Type local:Popover}">
            <Grid>
                <ContentPresenter Content="{TemplateBinding Target}" />
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   PopupAnimation="Fade"
                   IsOpen="{Binding IsOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                   PlacementTarget="{TemplateBinding PlacementTarget}"
                   Placement="{TemplateBinding Placement}"
                   HorizontalOffset="{TemplateBinding HorizontalOffset}"
                   VerticalOffset="{TemplateBinding VerticalOffset}"
                   StaysOpen="{TemplateBinding StaysOpen}">
                <Border x:Name="PART_Surface"
                        Focusable="True"
                        KeyboardNavigation.TabNavigation="Cycle"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Padding="{TemplateBinding Padding}">
                    <ContentPresenter Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                      ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                </Border>
            </Popup>
            </Grid>
        </ControlTemplate>
    </ContentControl.Template>
</ContentControl>
